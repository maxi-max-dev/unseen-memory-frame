{
  "name": "memory-frame-demo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@cloudbase/cli": "^3.8.3"
  }
}
